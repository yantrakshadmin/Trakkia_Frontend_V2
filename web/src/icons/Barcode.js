import React from 'react';
import {yantraColors} from '../helpers/yantraColors';

const Download = () => {
  return <i className="fas fa-barcode" style={{color: yantraColors['primary'], fontSize: 15, margin:'auto 5px'}} />;
};

export default Download;
