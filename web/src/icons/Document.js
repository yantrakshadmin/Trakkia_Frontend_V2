import React from 'react';

const Document = ({ color }) => {
  return <i className='fas fa-file' style={{ color, fontSize: 15, margin:'auto 5px', marginRight: '5px' }} />;
};

export default Document;
