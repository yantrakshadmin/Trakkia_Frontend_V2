import React from 'react';

const Print = ({color}) => {
  return (
    <i className="fas fa-print" style={{color: '#40e0d0', fontSize: 15, margin:'auto 5px', marginRight: '5px'}}></i>
  );
};

export default Print;
